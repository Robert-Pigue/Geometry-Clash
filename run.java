package h;

import javafx.application.Application;
/**
 * Class that runs the application.
 * @author Olivia
 * @version 1.0
 */
public class run {
    /**
     * Method that is run that starts the project.
     * @param args used to work with the command line.
     */
    public static void main(String[] args) {
        Application.launch(MainApp.class);
    }
}